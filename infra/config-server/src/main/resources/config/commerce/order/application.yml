server:
  port: 0

spring:
  application:
    name: order
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    username: smart
    password: secret
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/smart